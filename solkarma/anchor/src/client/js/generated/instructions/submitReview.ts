/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { SOLKARMA_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const SUBMIT_REVIEW_DISCRIMINATOR = new Uint8Array([
  106, 30, 50, 83, 89, 46, 213, 239,
]);

export function getSubmitReviewDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SUBMIT_REVIEW_DISCRIMINATOR
  );
}

export type SubmitReviewInstruction<
  TProgram extends string = typeof SOLKARMA_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountTargetAccount extends string | AccountMeta<string> = string,
  TAccountTargetProfile extends string | AccountMeta<string> = string,
  TAccountReviewAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountTargetAccount extends string
        ? ReadonlyAccount<TAccountTargetAccount>
        : TAccountTargetAccount,
      TAccountTargetProfile extends string
        ? WritableAccount<TAccountTargetProfile>
        : TAccountTargetProfile,
      TAccountReviewAccount extends string
        ? WritableAccount<TAccountReviewAccount>
        : TAccountReviewAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SubmitReviewInstructionData = {
  discriminator: ReadonlyUint8Array;
  target: Address;
  message: string;
  rating: number;
};

export type SubmitReviewInstructionDataArgs = {
  target: Address;
  message: string;
  rating: number;
};

export function getSubmitReviewInstructionDataEncoder(): Encoder<SubmitReviewInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['target', getAddressEncoder()],
      ['message', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['rating', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SUBMIT_REVIEW_DISCRIMINATOR })
  );
}

export function getSubmitReviewInstructionDataDecoder(): Decoder<SubmitReviewInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['target', getAddressDecoder()],
    ['message', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['rating', getU8Decoder()],
  ]);
}

export function getSubmitReviewInstructionDataCodec(): Codec<
  SubmitReviewInstructionDataArgs,
  SubmitReviewInstructionData
> {
  return combineCodec(
    getSubmitReviewInstructionDataEncoder(),
    getSubmitReviewInstructionDataDecoder()
  );
}

export type SubmitReviewAsyncInput<
  TAccountUser extends string = string,
  TAccountTargetAccount extends string = string,
  TAccountTargetProfile extends string = string,
  TAccountReviewAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  targetAccount: Address<TAccountTargetAccount>;
  targetProfile?: Address<TAccountTargetProfile>;
  reviewAccount?: Address<TAccountReviewAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  target: SubmitReviewInstructionDataArgs['target'];
  message: SubmitReviewInstructionDataArgs['message'];
  rating: SubmitReviewInstructionDataArgs['rating'];
};

export async function getSubmitReviewInstructionAsync<
  TAccountUser extends string,
  TAccountTargetAccount extends string,
  TAccountTargetProfile extends string,
  TAccountReviewAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLKARMA_PROGRAM_ADDRESS,
>(
  input: SubmitReviewAsyncInput<
    TAccountUser,
    TAccountTargetAccount,
    TAccountTargetProfile,
    TAccountReviewAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  SubmitReviewInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountTargetAccount,
    TAccountTargetProfile,
    TAccountReviewAccount,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLKARMA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    targetAccount: { value: input.targetAccount ?? null, isWritable: false },
    targetProfile: { value: input.targetProfile ?? null, isWritable: true },
    reviewAccount: { value: input.reviewAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.targetProfile.value) {
    accounts.targetProfile.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 114, 111, 102, 105, 108, 101])
        ),
        getAddressEncoder().encode(expectAddress(accounts.targetAccount.value)),
      ],
    });
  }
  if (!accounts.reviewAccount.value) {
    accounts.reviewAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([114, 101, 118, 105, 101, 119])
        ),
        getAddressEncoder().encode(expectAddress(accounts.user.value)),
        getAddressEncoder().encode(expectSome(args.target)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.targetAccount),
      getAccountMeta(accounts.targetProfile),
      getAccountMeta(accounts.reviewAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSubmitReviewInstructionDataEncoder().encode(
      args as SubmitReviewInstructionDataArgs
    ),
    programAddress,
  } as SubmitReviewInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountTargetAccount,
    TAccountTargetProfile,
    TAccountReviewAccount,
    TAccountSystemProgram
  >);
}

export type SubmitReviewInput<
  TAccountUser extends string = string,
  TAccountTargetAccount extends string = string,
  TAccountTargetProfile extends string = string,
  TAccountReviewAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  targetAccount: Address<TAccountTargetAccount>;
  targetProfile: Address<TAccountTargetProfile>;
  reviewAccount: Address<TAccountReviewAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  target: SubmitReviewInstructionDataArgs['target'];
  message: SubmitReviewInstructionDataArgs['message'];
  rating: SubmitReviewInstructionDataArgs['rating'];
};

export function getSubmitReviewInstruction<
  TAccountUser extends string,
  TAccountTargetAccount extends string,
  TAccountTargetProfile extends string,
  TAccountReviewAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLKARMA_PROGRAM_ADDRESS,
>(
  input: SubmitReviewInput<
    TAccountUser,
    TAccountTargetAccount,
    TAccountTargetProfile,
    TAccountReviewAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): SubmitReviewInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountTargetAccount,
  TAccountTargetProfile,
  TAccountReviewAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLKARMA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    targetAccount: { value: input.targetAccount ?? null, isWritable: false },
    targetProfile: { value: input.targetProfile ?? null, isWritable: true },
    reviewAccount: { value: input.reviewAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.targetAccount),
      getAccountMeta(accounts.targetProfile),
      getAccountMeta(accounts.reviewAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSubmitReviewInstructionDataEncoder().encode(
      args as SubmitReviewInstructionDataArgs
    ),
    programAddress,
  } as SubmitReviewInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountTargetAccount,
    TAccountTargetProfile,
    TAccountReviewAccount,
    TAccountSystemProgram
  >);
}

export type ParsedSubmitReviewInstruction<
  TProgram extends string = typeof SOLKARMA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    targetAccount: TAccountMetas[1];
    targetProfile: TAccountMetas[2];
    reviewAccount: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: SubmitReviewInstructionData;
};

export function parseSubmitReviewInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSubmitReviewInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      targetAccount: getNextAccount(),
      targetProfile: getNextAccount(),
      reviewAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSubmitReviewInstructionDataDecoder().decode(instruction.data),
  };
}
